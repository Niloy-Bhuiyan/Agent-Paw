# Pets assets

The pet system is fully procedural — cats are rendered at runtime by
`src/animations/pixel-cat/`, so no sprite files are required here.

Drop licensed sprite sheets into this folder if you want to swap the
procedural renderer for bitmap frames, and document their source and license
in the root `ASSETS.md`.
